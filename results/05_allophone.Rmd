---
title: "Allophone"
output: html_document
---

- 異音の分析なんだが、ツッコミから始まるアンチパターンに陥っている。
- ポジティブな、理論的な貢献を視座高くして考えるべき。

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(dplyr)
library(ggplot2)
library(readr)
library(stringr)
library(tidyr)
source("script/subject_info.R")
source("script/geom_flat_violin.R")
source("script/results_pitch.R")
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
original = read_csv("csv/illusory-vowel-keihan.csv") %>% extract_columns(data_src = "sone") %>% select("subj_id") %>% unlist
new = read_csv("csv/illusory-vowel-keihan-new.csv") %>% extract_columns(data_src = "sone") %>% select("subj_id") %>% unlist
setdiff(new, original)
original
# new

# allophone_sone <- read_csv("csv/illusory-vowel-keihan.csv") %>%
#   filter(task == "axb") %>%
#   select(c(run_id, item_id, is_correct, rt, trial_index)) %>%
#   mutate(data_src = "sone")
# allophone_sone %>% head
```

```{r}
allophone_cw <- read_csv("csv/illusory-vowel-keihan-cw.csv") %>%
  filter(task == "axb") %>%
  select(c(run_id, item_id, is_correct, rt, trial_index)) %>%
  mutate(data_src = "sone")

head(allophone_sone)
```

```{r}
# 統合と列生成
# results_pitch <-
allophone_sone %>%
  left_join(devoicing_annotation_subj, by = c("run_id", "data_src")) %>%
  merge(item_list, on = "item_id") %>%
  filter(type == "target") %>% head
  # allophone の実験は "target"
  drop_na() %>%
  mutate(
    is_correct = as.logical(is_correct),
    rt = as.numeric(rt),
    voiced = as.numeric(voiced),
    devoiced = as.numeric(devoiced)
  )
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
